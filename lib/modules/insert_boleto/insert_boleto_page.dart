import 'package:flutter/material.dart';

class InsertBoleto extends StatelessWidget {
  const InsertBoleto({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Insert Boleto'),
      ),
    );
  }
}
